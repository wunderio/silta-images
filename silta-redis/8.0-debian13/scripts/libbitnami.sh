#!/bin/bash
# Stub for Bitnami's libbitnami.sh — only needed if entrypoint.sh is used standalone

# shellcheck disable=SC1091
. /opt/bitnami/scripts/liblog.sh

print_welcome_page() { :; }
print_image_welcome_page() { :; }
